# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


GET   /ads/:id          ch.simpleswisshackers.backend.controllers.AdController.showAd(id: Int)
POST  /ads              ch.simpleswisshackers.backend.controllers.AdController.addAd()


# PUT /user/:fid
# { token: string, push: string }

# POST /ads/query
# { fid: string, token: string, latitude: double, longitude: double }
# -> { results: [ ads ] }



